{
  "_comment": "Syscall filter for the QA agent container. It constrains syscalls, which is a different question from which hosts the process may reach: connect(2) is either allowed or the process has no network at all.",
  "defaultAction": "SCMP_ACT_ERRNO",
  "architectures": ["SCMP_ARCH_X86_64"],
  "syscalls": [
    {
      "names": ["read", "write", "openat", "close", "fstat", "mmap", "brk", "rt_sigaction", "socket", "connect", "sendto", "recvfrom", "exit_group"],
      "action": "SCMP_ACT_ALLOW"
    }
  ]
}
